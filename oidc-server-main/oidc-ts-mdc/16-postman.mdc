# Title
Colección Postman mínima (referencia)

- GET `http://localhost:4000/api/me` (401 si no autenticado)
- GET `http://localhost:4000/auth/login` (sigue redirección)
- GET `http://localhost:5000/.well-known/openid-configuration`
- GET `http://localhost:5000/_admin/users` (DEV)
